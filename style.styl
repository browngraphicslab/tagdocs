zero = 0px
bodyFont = 16px
demoLinkColor = #aaaaff
orangeColor = #ff9614
images = './images/'

// functions and mixins

createUrl(imgName)
    if mixin
        background url(images+imgName)
    else
        url(images+imgName)
        
createLinGrad(startcolor, endcolor)
    background -webkit-gradient(linear, left top, left bottom, from(startcolor), to(endcolor))
    background linear-gradient(to bottom, startcolor, endcolor)
    
border-radius(r)
    -moz-border-radius r
    -webkit-border-radius r
    -o-border-radius r
    -ms-border-radius r
    -khtml-border-radius r
    border-radius r

sectionOffset(t)
	position absolute
	margin-top t

// styling

*
    margin zero
    padding zero

a
	text-decoration none
	color white

a.demo
	color demoLinkColor

body
	background createUrl('pattern1.png')
	color white

#contentWrapper
	width 1100px
	height 100%
	margin 0 auto
	createLinGrad(rgba(50,50,50,0.9), rgba(30,30,30,0.9))
	font-family Verdana, sans-serif
	font-size bodyFont

#content
	width 800px
	padding-left 20px
	padding-top 20px
	margin-left 260px
	border-left 2px solid orangeColor

#leftbar
	position absolute
	width 240px
	padding-left 20px
	padding-top 15px
	border-right 2px solid orangeColor

span
	font-family courier
	font-size (bodyFont - 1)
	color #dddddd

#topbar
	width 100%
	height 110px
	padding-top 20px
	border-bottom 2px solid orangeColor

#top-text
	padding-left 20px
	font-size 100px
	color rgba(20,150,255,1.0)

#top-subtext
	padding-left 30px
	font-size 20px
	color #ffffff

h2.linklist
	color #ee7514

li.linklistitem
	padding-left 10px

h1.linkhead
	color orangeColor
	text-decoration:underline

ul
	list-style none

.page
	display none

.pageanch
	cursor pointer

ol
	margin-left 40px

ol.nostyle
	list-style none

pre.prettyprint, code.prettyprint
	border-radius 8px
	padding-top 5px
	padding-bottom 5px
	padding-left 5px

#widgetContainer
    text-align center

#widgetContainer textarea
    background #222222
    color #ffffff
    width 80%
    height 400px

#widgetContainer .button
    display inline-block
    cursor pointer
    color #ffffff
    margin 0px 40px 0px 40px

.descContainer
    width 80%
    margin-left 10%

.container
    width 600px
    height 400px
    background-color black
    border 1px solid black
    color white

#mainSeadragonContainer
    margin-left 10%

.hammertestBox
    background-color green
    text-align center
    color white
    border-radius 10px
    width 400px
    height 100px
    margin 40px

#stormtrooper
	position absolute
	margin-left 400px

#thumbnail
	margin-left 400px
	margin-top 280px

#annotationContainer
	position absolute
	width 350px

#annotationHeader
	sectionOffset(0px)

#annotations
	sectionOffset(20px)

#imageHeader
	sectionOffset(60px)

#images
	sectionOffset(80px)

#images img
	width 200px

#footnoteHeader
	sectionOffset(200px)

#footnotes
	sectionOffset(220px)

#footnoteAddHeader
	sectionOffset(260px)

#footnoteAdd
	sectionOffset(280px)

#pscanvas
	width 800px
	height 400px
	border 1px solid rgba(20,150,255,1.0)
	position

#closeThreshDiv, #threshDiv
	display inline-block